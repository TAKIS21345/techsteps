import React from 'react';
import ChatDashboard from './ChatDashboard';

const DashboardPage: React.FC = () => {
  // Use the new ChatGPT-style interface
  return <ChatDashboard />;
};

export default DashboardPage;